import * as React from 'react';
import { Typography } from '@mui/material';

const DomainAddForm: React.FC = () => {
  return (
    <Typography component="div" variant="body1">
      Adding a new domain is not possible yet.
    </Typography>
  );
};

export default DomainAddForm;
