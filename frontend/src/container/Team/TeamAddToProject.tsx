import * as React from 'react';
import { Typography } from '@mui/material';

const TeamAddToProject: React.FC = () => {
  return (
    <Typography component="div" variant="body1">
      The possibility to add a team to a project and/or domain is not
      implemented yet.
    </Typography>
  );
};

export default TeamAddToProject;
